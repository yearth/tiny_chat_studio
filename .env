# .env (可提交到Git)
# 此文件包含环境变量的文档说明，但不包含敏感值
# 敏感值应在 .env.local（本地开发）或生产环境变量（如Vercel UI）中设置

# Prisma 数据库连接
# 完整连接字符串应在 .env.local 或生产环境变量中设置
# DATABASE_URL="postgresql://user:password@host:port/database?sslmode=require"

# AI 提供商 API 密钥（在 .env.local 或生产环境变量中设置）
# OPENAI_API_KEY=""
DEEPSEEK_API_KEY=""
DASHSCOPE_API_KEY=""
OPENROUTER_API_KEY=""

# Google OAuth 配置
# 客户端ID通常不敏感，但为了一致性也可以使用占位符
GOOGLE_CLIENT_ID="your-google-client-id-here"
# GOOGLE_CLIENT_SECRET="" # 在 .env.local 或生产环境变量中设置

# NextAuth 配置
# NEXTAUTH_URL="http://localhost:3000" # 本地开发默认值，在生产环境中覆盖
AUTH_SECRET="" # 在 .env.local 或生产环境变量中设置

# 可选：用于本地开发/测试的默认用户ID
# USER_ID="" # 如需用于本地测试，在 .env.local 中设置